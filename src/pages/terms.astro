---
import BaseLayout from "../layouts/BaseLayout.astro";
import { content } from "../config/content";
---

<BaseLayout title={content.terms.title} description={content.terms.description} noindex>
  <section class="section">
    <div class="container">
      <h1>{content.terms.heading}</h1>

      <div class="legal-notice">
        <strong>{content.terms.notice.label}</strong> {content.terms.notice.body}
      </div>

      <p class="text-muted">{content.terms.lastUpdated}</p>

      {
        content.terms.sections.map((section) => (
          <>
            <h2>{section.heading}</h2>
            <p>
              {section.body}
              {section.email && (
                <>
                  {" "}<a href={`mailto:${section.email}`}>{section.email}</a>.
                </>
              )}
            </p>
          </>
        ))
      }
    </div>
  </section>
</BaseLayout>
